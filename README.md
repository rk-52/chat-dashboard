# chat-dashboard
Real-time notification system using GraphQL mutations, backend event processing, and Firebase Cloud Messaging (FCM). The project demonstrates event-driven architecture for sending instant push notifications from server-side actions to client devices
